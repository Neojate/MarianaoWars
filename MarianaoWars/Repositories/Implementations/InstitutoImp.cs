using MarianaoWars.Data;
using MarianaoWars.Models;
using MarianaoWars.Repositories.Interfaces;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace MarianaoWars.Repositories.Implementations
{

    public class InstitutoImp //: IInstitutoRepository
    {

        /*//private MarianaoContext db = new MarianaoContext();


        public Instituto CreateInstituto(Instituto createdInstituto)
        {
            //db.Add(createdInstituto);

            //db.SaveChanges();

            return createdInstituto;
        }

        public void DeleteInstituto(Guid id)
        {
            Instituto institutoToDelete = GetInstituto(id);

            if (institutoToDelete != null)
                //db.Instituto.Remove(institutoToDelete);
        }

        public Instituto GetInstituto(Guid id)
        {
            //return db.Instituto.FirstOrDefault(x => x.ID == id);
        }

        public IEnumerable<Instituto> GetInstitutos()
        {
            //return db.Instituto.ToList();
        }

        public void UpdateInstituto(Instituto updatedInstituto)
        {
            //db.Update(updatedInstituto);

            //db.SaveChanges();
        }*/
    }
}
